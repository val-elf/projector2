
/*import { BrushFiller } from 'picture-editor/build/worker';

const brushFiller = new BrushFiller();

addEventListener('message', response => {
  const { data } = response;
  brushFiller.processCommand(data);
});*/
